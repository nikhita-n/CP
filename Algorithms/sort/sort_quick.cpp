//disadvantages:
    -linearithmic number of string compares(not linear)
    -has to rescan many characters in keys with long prefix matches//(if the characetrs are same, it has to recheck

//2NlnN string compares on average
    -costly for keys with long common prefixes
    